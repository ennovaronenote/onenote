import { AuthenticationClient } from "./AuthenticationClient";

export default function duplicateStudentProfile(
  client: AuthenticationClient,
  context: any
) {
  console.log("Consider it duplicated.");
}
