import { NextPage } from "next";

/**
 * Page to render the home / landing page.
 * @group Pages
 */
const Home: NextPage = () => {
  return (
    <div>
      <h1>Home Page</h1>
    </div>
  );
};

export default Home;
