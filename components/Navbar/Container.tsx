import NavbarLinks from "./Links";

/**
 * Container component to render navigation links.
 */
function NavbarContainer() {
  return (
    <div className="container mx-auto">
      <NavbarLinks />
    </div>
  );
}

export default NavbarContainer;
